<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sysmex Forecast Data Entry</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif;
            background: linear-gradient(135deg, #f5f7fa 0%, #e8eef3 100%);
            min-height: 100vh;
            padding: 20px;
            line-height: 1.6;
        }

        .container {
            max-width: 900px;
            margin: 0 auto;
            background: white;
            border-radius: 12px;
            box-shadow: 0 4px 20px rgba(0, 61, 165, 0.08);
            overflow: hidden;
        }

        /* Header */
        .header {
            background: linear-gradient(135deg, #005bac 0%, #003d73 100%);
            color: white;
            padding: 40px 30px;
            text-align: center;
            position: relative;
        }

        .logo {
            max-width: 200px;
            height: auto;
            display: inline-block;
            margin-bottom: 20px;
            background: white;
            padding: 12px 20px;
            border-radius: 8px;
        }

        .header h1 {
            font-size: 28px;
            font-weight: 600;
            margin-bottom: 8px;
        }

        .header p {
            font-size: 15px;
            opacity: 0.95;
        }

        /* Admin Button */
        .admin-btn {
            position: absolute;
            top: 20px;
            right: 20px;
            background: rgba(255,255,255,0.2);
            border: 2px solid white;
            color: white;
            padding: 8px 16px;
            border-radius: 6px;
            cursor: pointer;
            font-size: 12px;
            font-weight: 600;
            transition: all 0.3s;
        }

        .admin-btn:hover {
            background: white;
            color: #005bac;
        }

        /* Content */
        .content {
            padding: 40px 30px;
        }

        /* Screens */
        .screen {
            display: none;
        }

        .screen.active {
            display: block;
            animation: fadeIn 0.5s ease-in;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* Welcome */
        .welcome-title {
            text-align: center;
            color: #005bac;
            font-size: 32px;
            margin-bottom: 16px;
        }

        .welcome-subtitle {
            text-align: center;
            color: #6b7280;
            font-size: 18px;
            margin-bottom: 48px;
        }

        /* Country Grid */
        .country-grid {
            display: grid;
            grid-template-columns: repeat(5, 1fr);
            gap: 12px;
            max-width: 700px;
            margin: 30px auto;
        }

        .country-card {
            background: white;
            border: 2px solid #e5e7eb;
            border-radius: 10px;
            padding: 20px 12px;
            text-align: center;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .country-card:hover {
            border-color: #00b8ee;
            transform: translateY(-3px);
            box-shadow: 0 6px 12px rgba(0, 184, 238, 0.3);
        }

        .country-flag {
            font-size: 32px;
            margin-bottom: 8px;
        }

        .country-code {
            font-size: 18px;
            font-weight: 700;
            color: #005bac;
        }

        /* Form */
        .form-group {
            margin-bottom: 24px;
        }

        label {
            display: block;
            font-weight: 600;
            margin-bottom: 8px;
            color: #1f2937;
            font-size: 14px;
        }

        .required {
            color: #ef4444;
            margin-left: 2px;
        }

        input, textarea, select {
            width: 100%;
            padding: 12px 14px;
            border: 2px solid #e5e7eb;
            border-radius: 8px;
            font-size: 15px;
            font-family: inherit;
            transition: all 0.2s ease;
            background: white;
        }

        input:focus, textarea:focus, select:focus {
            outline: none;
            border-color: #00b8ee;
            box-shadow: 0 0 0 4px rgba(0, 184, 238, 0.15);
        }

        /* Info Bar */
        .info-bar {
            background: #e6f4fb;
            padding: 20px 30px;
            border-radius: 8px;
            margin-bottom: 30px;
        }

        .info-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 20px;
        }

        .info-item {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .info-icon {
            font-size: 24px;
        }

        .info-text strong {
            display: block;
            color: #005bac;
            font-size: 11px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 4px;
            font-weight: 600;
        }

        .info-text span {
            font-size: 15px;
            color: #1a1a1a;
            font-weight: 500;
        }

        /* Quarter Sections */
        .quarter-section {
            background: #f9fafb;
            padding: 24px;
            border-radius: 10px;
            margin-bottom: 24px;
            border: 2px solid #e5e7eb;
        }

        .quarter-header {
            margin-bottom: 20px;
            padding-bottom: 16px;
            border-bottom: 2px solid #e5e7eb;
        }

        .quarter-header h3 {
            color: #005bac;
            font-size: 18px;
            font-weight: 600;
        }

        .quarter-period {
            color: #6b7280;
            font-size: 14px;
            margin-top: 4px;
        }

        .fields-row {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
            gap: 18px;
            margin-bottom: 18px;
        }

        .input-group {
            position: relative;
        }

        .input-suffix {
            position: absolute;
            right: 14px;
            top: 50%;
            transform: translateY(-50%);
            color: #6b7280;
            font-size: 14px;
            font-weight: 600;
            pointer-events: none;
        }

        input[type="number"] {
            padding-right: 42px;
        }

        /* Buttons */
        .btn {
            padding: 14px 28px;
            border: none;
            border-radius: 10px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s ease;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
        }

        .btn-primary {
            background: linear-gradient(135deg, #005bac 0%, #003d73 100%);
            color: white;
            width: 100%;
        }

        .btn-primary:hover:not(:disabled) {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(0, 91, 172, 0.4);
        }

        .btn-primary:disabled {
            background: #9ca3af;
            cursor: not-allowed;
        }

        .btn-secondary {
            background: white;
            color: #005bac;
            border: 2px solid #005bac;
        }

        .btn-secondary:hover {
            background: #e6f4fb;
        }

        .btn-danger {
            background: #ef4444;
            color: white;
        }

        .btn-danger:hover {
            background: #dc2626;
        }

        .spinner {
            display: inline-block;
            width: 16px;
            height: 16px;
            border: 2px solid rgba(255, 255, 255, 0.3);
            border-top-color: white;
            border-radius: 50%;
            animation: spin 0.6s linear infinite;
        }

        @keyframes spin {
            to { transform: rotate(360deg); }
        }

        /* Alerts */
        .alert {
            padding: 16px 20px;
            border-radius: 8px;
            margin-bottom: 24px;
            display: none;
            animation: slideDown 0.3s ease-out;
        }

        @keyframes slideDown {
            from { opacity: 0; transform: translateY(-10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .alert.show {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .alert-success {
            background: #d1f5e5;
            border: 2px solid #00a74d;
            color: #005c29;
        }

        .alert-error {
            background: #fee2e2;
            border: 2px solid #ef4444;
            color: #991b1b;
        }

        /* Admin Panel */
        .admin-table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
        }

        .admin-table th {
            background: #005bac;
            color: white;
            padding: 12px;
            text-align: left;
            font-weight: 600;
        }

        .admin-table td {
            padding: 12px;
            border-bottom: 1px solid #e5e7eb;
        }

        .admin-table tr:hover {
            background: #f9fafb;
        }

        .action-btn {
            padding: 6px 12px;
            font-size: 13px;
            margin-right: 8px;
        }

        /* Footer */
        .footer {
            text-align: center;
            padding: 24px;
            color: #6b7280;
            font-size: 13px;
            border-top: 2px solid #f3f4f6;
            background: #f9fafb;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .country-grid {
                grid-template-columns: repeat(3, 1fr);
            }
            .fields-row {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header -->
        <div class="header">
            <button class="admin-btn" onclick="showAdminLogin()">⚙️ Admin</button>
            <img src="https://www.conferenceharvester.com/uploads/harvester/photos/RDEMYCDB-ExhibitorLogo-556568(1).png" 
                 alt="Sysmex" 
                 class="logo">
            <h1>Forecast Data Entry</h1>
            <p>Monthly Data Collection System</p>
        </div>

        <!-- Content -->
        <div class="content">
            <!-- Alerts -->
            <div id="alertSuccess" class="alert alert-success">
                <span style="font-size: 20px;">✅</span>
                <span id="alertSuccessText"></span>
            </div>
            <div id="alertError" class="alert alert-error">
                <span style="font-size: 20px;">❌</span>
                <span id="alertErrorText"></span>
            </div>

            <!-- Screen 1: Welcome & Email -->
            <div id="screen1" class="screen active">
                <h2 class="welcome-title">Welcome to Sysmex Forecast System</h2>
                <p class="welcome-subtitle">Monthly Data Collection for EMEA Controllers</p>

                <div style="max-width: 500px; margin: 40px auto;">
                    <div class="form-group">
                        <label for="userEmail">Email Address <span class="required">*</span></label>
                        <input type="email" 
                               id="userEmail" 
                               placeholder="your.email@sysmex.com" 
                               required>
                    </div>

                    <button type="button" class="btn btn-primary" onclick="validateEmail()">
                        Continue →
                    </button>
                </div>
            </div>

            <!-- Screen 2: Country Selection -->
            <div id="screen2" class="screen">
                <h2 class="welcome-title">Select your country</h2>
                <p class="welcome-subtitle">Choose the country you represent</p>

                <div class="country-grid" id="countryGrid">
                    <!-- Countries loaded from storage -->
                </div>
            </div>

            <!-- Screen 3: Data Entry Form -->
            <div id="screen3" class="screen">
                <div class="info-bar">
                    <div class="info-grid">
                        <div class="info-item">
                            <span class="info-icon">📅</span>
                            <div class="info-text">
                                <strong>Fiscal Year</strong>
                                <span id="fiscalYearDisplay">--</span>
                            </div>
                        </div>
                        <div class="info-item">
                            <span class="info-icon">📈</span>
                            <div class="info-text">
                                <strong>Fiscal Month</strong>
                                <span id="fiscalMonthDisplay">--</span>
                            </div>
                        </div>
                        <div class="info-item">
                            <span class="info-icon">⏰</span>
                            <div class="info-text">
                                <strong>Deadline</strong>
                                <span id="deadlineDisplay">05</span>
                            </div>
                        </div>
                    </div>
                </div>

                <form id="forecastForm">
                    <div id="quartersContainer"></div>
                    <button type="submit" class="btn btn-primary" id="submitBtn">
                        <span id="submitBtnText">✅ Submit Data</span>
                    </button>
                </form>
            </div>

            <!-- Screen 4: Admin Login -->
            <div id="screenAdminLogin" class="screen">
                <h2 class="welcome-title">Admin Panel</h2>
                <p class="welcome-subtitle">Enter password to continue</p>

                <div style="max-width: 400px; margin: 40px auto;">
                    <div class="form-group">
                        <label for="adminPassword">Password</label>
                        <input type="password" id="adminPassword" placeholder="Enter password">
                    </div>
                    <button type="button" class="btn btn-primary" onclick="validateAdminPassword()">
                        Login
                    </button>
                </div>
            </div>

            <!-- Screen 5: Admin Panel -->
            <div id="screenAdmin" class="screen">
                <h2 class="welcome-title">Admin Panel</h2>
                <button class="btn btn-secondary" onclick="goToScreen(1)" style="margin-bottom: 20px;">
                    ← Back to Form
                </button>

                <!-- Countries Management -->
                <h3 style="color: #003DA5; margin: 30px 0 20px;">Countries Management</h3>
                
                <div style="background: #f9fafb; padding: 20px; border-radius: 10px; margin-bottom: 20px;">
                    <h4 style="margin-bottom: 15px;">Add New Country</h4>
                    <div style="display: grid; grid-template-columns: 1fr 1fr 1fr auto; gap: 10px;">
                        <input type="text" id="newCountryCode" placeholder="Code (e.g., SPT)">
                        <input type="text" id="newCountryFlag" placeholder="Flag (e.g., 🇵🇹)">
                        <input type="number" id="newCountryCompanyId" placeholder="Company ID">
                        <button class="btn btn-primary" onclick="addCountry()">Add</button>
                    </div>
                </div>

                <table class="admin-table">
                    <thead>
                        <tr>
                            <th>Flag</th>
                            <th>Code</th>
                            <th>Company ID</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody id="countriesTable">
                    </tbody>
                </table>

                <!-- Authorized Emails Management -->
                <h3 style="color: #003DA5; margin: 30px 0 20px;">Authorized Emails</h3>
                
                <div style="background: #f9fafb; padding: 20px; border-radius: 10px; margin-bottom: 20px;">
                    <h4 style="margin-bottom: 15px;">Add Authorized Email</h4>
                    <div style="display: grid; grid-template-columns: 1fr 1fr auto; gap: 10px;">
                        <input type="email" id="newEmail" placeholder="Email address">
                        <input type="text" id="newEmailName" placeholder="Name (optional)">
                        <button class="btn btn-primary" onclick="addEmail()">Add</button>
                    </div>
                </div>

                <table class="admin-table">
                    <thead>
                        <tr>
                            <th>Email</th>
                            <th>Name</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody id="emailsTable">
                    </tbody>
                </table>
            </div>
        </div>

        <!-- Footer -->
        <div class="footer">
            Automated Data Collection System • Sysmex EMEA © 2026
        </div>
    </div>

    <script>
        // ============================================
        // CONFIGURACIÓ
        // ============================================
        const API_URL = 'https://default66b9ec7f68a64d5ba8fea7bac3927e.7c.environment.api.powerplatform.com:443/powerautomate/automations/direct/workflows/a011eebbb19044a99b95018eacb03d9c/triggers/manual/paths/invoke?api-version=1&sp=%2Ftriggers%2Fmanual%2Frun&sv=1.0&sig=oEHwnt2OWc9Mpc2AzkAc1GLaq6Ppk2d5oCh8fjdnpTU';
        const ADMIN_PASSWORD = 'x30';

        // ============================================
        // ESTAT GLOBAL
        // ============================================
        let userEmail = '';
        let selectedCountry = null;
        let selectedCompanyId = null;
        let fiscalInfo = {};
        let quarters = [];

        // ============================================
        // LOCAL STORAGE (simulant base de dades)
        // ============================================
        function getCountries() {
            const stored = localStorage.getItem('sysmex_countries');
            return stored ? JSON.parse(stored) : [
                { code: 'SPT', flag: '🇵🇹', companyId: 3049 },
                { code: 'SIT', flag: '🇮🇹', companyId: 3036 },
                { code: 'SFR', flag: '🇫🇷', companyId: 3008 },
                { code: 'SES', flag: '🇪🇸', companyId: 3023 },
                { code: 'SGR', flag: '🇬🇷', companyId: 3050 }
            ];
        }

        function saveCountries(countries) {
            localStorage.setItem('sysmex_countries', JSON.stringify(countries));
        }

        function getAuthorizedEmails() {
            const stored = localStorage.getItem('sysmex_emails');
            return stored ? JSON.parse(stored) : [];
        }

        function saveAuthorizedEmails(emails) {
            localStorage.setItem('sysmex_emails', JSON.stringify(emails));
        }

        // ============================================
        // FUNCIONS DE NAVEGACIÓ
        // ============================================
        function goToScreen(screenNumber) {
            document.querySelectorAll('.screen').forEach(s => s.classList.remove('active'));
            document.getElementById(`screen${screenNumber}`).classList.add('active');
            window.scrollTo({ top: 0, behavior: 'smooth' });
        }

        function validateEmail() {
            const emailInput = document.getElementById('userEmail');
            const email = emailInput.value.trim().toLowerCase();
            
            if (!email) {
                showAlert('error', 'Please enter your email address.');
                return;
            }
            
            const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
            if (!emailRegex.test(email)) {
                showAlert('error', 'Please enter a valid email address.');
                return;
            }
            
            // Validate against authorized emails
            const authorizedEmails = getAuthorizedEmails();
            const isAuthorized = authorizedEmails.some(e => e.email.toLowerCase() === email);
            
            if (authorizedEmails.length > 0 && !isAuthorized) {
                showAlert('error', 'This email is not authorized. Please contact your administrator.');
                return;
            }
            
            userEmail = email;
            loadCountryGrid();
            goToScreen(2);
        }

        function loadCountryGrid() {
            const grid = document.getElementById('countryGrid');
            const countries = getCountries();
            
            grid.innerHTML = countries.map(c => `
                <div class="country-card" onclick="selectCountry('${c.code}', ${c.companyId})">
                    <div class="country-flag">${c.flag}</div>
                    <div class="country-code">${c.code}</div>
                </div>
            `).join('');
        }

        function selectCountry(country, companyId) {
            selectedCountry = country;
            selectedCompanyId = companyId;
            updateFiscalInfo();
            generateQuarterSections();
            goToScreen(3);
        }

        // ============================================
        // ADMIN FUNCTIONS
        // ============================================
        function showAdminLogin() {
            goToScreen('AdminLogin');
        }

        function validateAdminPassword() {
            const password = document.getElementById('adminPassword').value;
            if (password === ADMIN_PASSWORD) {
                loadAdminPanel();
                goToScreen('Admin');
                document.getElementById('adminPassword').value = '';
            } else {
                showAlert('error', 'Incorrect password.');
            }
        }

        function loadAdminPanel() {
            loadCountriesTable();
            loadEmailsTable();
        }

        function loadCountriesTable() {
            const tbody = document.getElementById('countriesTable');
            const countries = getCountries();
            
            tbody.innerHTML = countries.map((c, index) => `
                <tr>
                    <td style="font-size: 24px;">${c.flag}</td>
                    <td><strong>${c.code}</strong></td>
                    <td>${c.companyId}</td>
                    <td>
                        <button class="btn btn-danger action-btn" onclick="deleteCountry(${index})">Delete</button>
                    </td>
                </tr>
            `).join('');
        }

        function addCountry() {
            const code = document.getElementById('newCountryCode').value.trim().toUpperCase();
            const flag = document.getElementById('newCountryFlag').value.trim();
            const companyId = parseInt(document.getElementById('newCountryCompanyId').value);
            
            if (!code || !flag || !companyId) {
                showAlert('error', 'Please fill all fields.');
                return;
            }
            
            const countries = getCountries();
            countries.push({ code, flag, companyId });
            saveCountries(countries);
            loadCountriesTable();
            
            document.getElementById('newCountryCode').value = '';
            document.getElementById('newCountryFlag').value = '';
            document.getElementById('newCountryCompanyId').value = '';
            
            showAlert('success', 'Country added successfully!');
        }

        function deleteCountry(index) {
            if (!confirm('Are you sure you want to delete this country?')) return;
            
            const countries = getCountries();
            countries.splice(index, 1);
            saveCountries(countries);
            loadCountriesTable();
            showAlert('success', 'Country deleted successfully!');
        }

        function loadEmailsTable() {
            const tbody = document.getElementById('emailsTable');
            const emails = getAuthorizedEmails();
            
            if (emails.length === 0) {
                tbody.innerHTML = '<tr><td colspan="3" style="text-align: center; color: #6b7280;">No authorized emails. All emails are allowed.</td></tr>';
                return;
            }
            
            tbody.innerHTML = emails.map((e, index) => `
                <tr>
                    <td>${e.email}</td>
                    <td>${e.name || '-'}</td>
                    <td>
                        <button class="btn btn-danger action-btn" onclick="deleteEmail(${index})">Delete</button>
                    </td>
                </tr>
            `).join('');
        }

        function addEmail() {
            const email = document.getElementById('newEmail').value.trim().toLowerCase();
            const name = document.getElementById('newEmailName').value.trim();
            
            if (!email) {
                showAlert('error', 'Please enter an email address.');
                return;
            }
            
            const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
            if (!emailRegex.test(email)) {
                showAlert('error', 'Please enter a valid email address.');
                return;
            }
            
            const emails = getAuthorizedEmails();
            if (emails.some(e => e.email === email)) {
                showAlert('error', 'This email is already authorized.');
                return;
            }
            
            emails.push({ email, name });
            saveAuthorizedEmails(emails);
            loadEmailsTable();
            
            document.getElementById('newEmail').value = '';
            document.getElementById('newEmailName').value = '';
            
            showAlert('success', 'Email authorized successfully!');
        }

        function deleteEmail(index) {
            if (!confirm('Are you sure you want to remove this email?')) return;
            
            const emails = getAuthorizedEmails();
            emails.splice(index, 1);
            saveAuthorizedEmails(emails);
            loadEmailsTable();
            showAlert('success', 'Email removed successfully!');
        }

        // ============================================
        // CÀLCUL FISCAL
        // ============================================
        function calculateFiscalInfo() {
            const now = new Date();
            const calendarMonth = now.getMonth() + 1;
            const calendarYear = now.getFullYear();
            const fiscalYear = calendarMonth >= 4 ? calendarYear : calendarYear - 1;
            const fiscalMonth = calendarMonth >= 4 ? calendarMonth - 3 : calendarMonth + 9;
            
            if (fiscalMonth >= 1 && fiscalMonth <= 3) {
                quarters = [
                    { name: 'Q1', period: 'April - June' },
                    { name: 'Q2', period: 'July - September' }
                ];
            } else if (fiscalMonth >= 4 && fiscalMonth <= 6) {
                quarters = [
                    { name: 'Q2', period: 'July - September' },
                    { name: 'Q3', period: 'October - December' }
                ];
            } else if (fiscalMonth >= 7 && fiscalMonth <= 9) {
                quarters = [
                    { name: 'Q3', period: 'October - December' },
                    { name: 'Q4', period: 'January - March' }
                ];
            } else {
                quarters = [
                    { name: 'Q4', period: 'January - March' }
                ];
            }
            
            return { fiscalYear, fiscalMonth, calendarMonth };
        }

        function getMonthName(month) {
            const months = ['January', 'February', 'March', 'April', 'May', 'June', 'July', 'August', 'September', 'October', 'November', 'December'];
            return months[month - 1];
        }

        function getDeadline() {
            const now = new Date();
            const deadline = new Date(now.getFullYear(), now.getMonth(), 5);
            return deadline.toLocaleDateString('en-GB', { day: '2-digit', month: '2-digit', year: 'numeric' });
        }

        function updateFiscalInfo() {
            fiscalInfo = calculateFiscalInfo();
            const monthName = getMonthName(fiscalInfo.calendarMonth);
            
            document.getElementById('fiscalYearDisplay').textContent = `FY ${fiscalInfo.fiscalYear}`;
            document.getElementById('fiscalMonthDisplay').textContent = `${fiscalInfo.fiscalMonth} (${monthName})`;
            document.getElementById('deadlineDisplay').textContent = getDeadline();
        }

        function generateQuarterSections() {
            const container = document.getElementById('quartersContainer');
            container.innerHTML = '';
            
            quarters.forEach(quarter => {
                const section = document.createElement('div');
                section.className = 'quarter-section';
                
                section.innerHTML = `
                    <div class="quarter-header">
                        <h3>${quarter.name} FY ${fiscalInfo.fiscalYear}</h3>
                        <div class="quarter-period">${quarter.period}</div>
                    </div>
                    
                    <div class="fields-row">
                        <div class="form-group">
                            <label>External Sales <span class="required">*</span></label>
                            <div class="input-group">
                                <input type="number" step="0.01" min="0" required
                                       name="${quarter.name}_ExternalSales" placeholder="1250.50">
                                <span class="input-suffix">k€</span>
                            </div>
                        </div>
                        
                        <div class="form-group">
                            <label>Internal Sales <span class="required">*</span></label>
                            <div class="input-group">
                                <input type="number" step="0.01" min="0" required
                                       name="${quarter.name}_InternalSales" placeholder="450.25">
                                <span class="input-suffix">k€</span>
                            </div>
                        </div>
                        
                        <div class="form-group">
                            <label>Cost of Sales <span class="required">*</span></label>
                            <div class="input-group">
                                <input type="number" step="0.01" min="0" required
                                       name="${quarter.name}_CostOfSales" placeholder="850.00">
                                <span class="input-suffix">k€</span>
                            </div>
                        </div>
                    </div>
                    
                    <div class="fields-row">
                        <div class="form-group">
                            <label>Labor Cost <span class="required">*</span></label>
                            <div class="input-group">
                                <input type="number" step="0.01" min="0" required
                                       name="${quarter.name}_LaborCost" placeholder="320.75">
                                <span class="input-suffix">k€</span>
                            </div>
                        </div>
                        
                        <div class="form-group">
                            <label>General Expense <span class="required">*</span></label>
                            <div class="input-group">
                                <input type="number" step="0.01" min="0" required
                                       name="${quarter.name}_GeneralExpense" placeholder="180.00">
                                <span class="input-suffix">k€</span>
                            </div>
                        </div>
                        
                        <div class="form-group">
                            <label>Depreciation <span class="required">*</span></label>
                            <div class="input-group">
                                <input type="number" step="0.01" min="0" required
                                       name="${quarter.name}_Depreciation" placeholder="95.50">
                                <span class="input-suffix">k€</span>
                            </div>
                        </div>
                    </div>
                `;
                
                container.appendChild(section);
            });
        }

        // ============================================
        // ALERTS
        // ============================================
        function showAlert(type, message) {
            document.querySelectorAll('.alert').forEach(el => el.classList.remove('show'));
            const alertEl = document.getElementById(`alert${type.charAt(0).toUpperCase() + type.slice(1)}`);
            const textEl = document.getElementById(`alert${type.charAt(0).toUpperCase() + type.slice(1)}Text`);
            textEl.textContent = message;
            alertEl.classList.add('show');
            window.scrollTo({ top: 0, behavior: 'smooth' });
            
            if (type === 'success') {
                setTimeout(() => alertEl.classList.remove('show'), 5000);
            }
        }

        // ============================================
        // ENVIAMENT FORMULARI
        // ============================================
        document.getElementById('forecastForm').addEventListener('submit', async function(e) {
            e.preventDefault();
            
            const submitBtn = document.getElementById('submitBtn');
            const submitBtnText = document.getElementById('submitBtnText');
            const originalText = submitBtnText.innerHTML;
            
            submitBtn.disabled = true;
            submitBtnText.innerHTML = '<span class="spinner"></span> Submitting...';
            
            try {
                const data = {
                    Email: userEmail,
                    Country: selectedCountry,
                    CompanyID: selectedCompanyId,
                    FiscalYear: `FY ${fiscalInfo.fiscalYear}`,
                    FiscalMonth: fiscalInfo.fiscalMonth,
                    SubmissionDate: new Date().toISOString(),
                    Quarters: []
                };
                
                quarters.forEach(quarter => {
                    const quarterData = {
                        Quarter: quarter.name,
                        QuarterType: '',
                        ExternalSales: parseFloat(document.querySelector(`[name="${quarter.name}_ExternalSales"]`).value),
                        InternalSales: parseFloat(document.querySelector(`[name="${quarter.name}_InternalSales"]`).value),
                        CostOfSales: parseFloat(document.querySelector(`[name="${quarter.name}_CostOfSales"]`).value),
                        LaborCost: parseFloat(document.querySelector(`[name="${quarter.name}_LaborCost"]`).value),
                        GeneralExpense: parseFloat(document.querySelector(`[name="${quarter.name}_GeneralExpense"]`).value),
                        Depreciation: parseFloat(document.querySelector(`[name="${quarter.name}_Depreciation"]`).value),
                        Comments: ''
                    };
                    data.Quarters.push(quarterData);
                });
                
                console.log('📤 Sending data:', data);
                
                const response = await fetch(API_URL, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(data)
                });
                
                console.log('📨 Response:', response.status, response.statusText);
                
                if (!response.ok) {
                    throw new Error(`Error ${response.status}: ${response.statusText}`);
                }
                
                showAlert('success', `Data sent successfully for ${selectedCountry}! You will receive a confirmation email.`);
                
                setTimeout(() => {
                    document.getElementById('forecastForm').reset();
                    goToScreen(1);
                }, 2000);
                
            } catch (error) {
                console.error('❌ Error:', error);
                showAlert('error', `Error sending data: ${error.message}. Please try again.`);
            } finally {
                submitBtn.disabled = false;
                submitBtnText.innerHTML = originalText;
            }
        });
    </script>
</body>
</html>
